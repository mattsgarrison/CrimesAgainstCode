source 'http://rubygems.org'

gem 'rails', '3.1.1'

# Bundle edge Rails instead:
# gem 'rails',     :git => 'git://github.com/rails/rails.git'

gem 'sqlite3'

# Asset template engines
gem 'sass'
gem 'coffee-script'
gem 'therubyracer'
gem 'uglifier'
gem 'jquery-rails'

# Project specific gems:
gem 'thin'
gem 'haml'
gem 'rqrcode'
gem 'compass'
gem 'bourbon'
# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
gem 'capistrano'

# To use debugger
# gem 'ruby-debug19', :require => 'ruby-debug'

group :test do
  # Pretty printed test output
  gem 'turn', :require => false
end

# Added to project so the Rails console is fancy.
group :development do
  gem 'sqlite3'
  gem 'wirble'
  gem 'awesome_print'
  gem 'hirb'
  gem 'interactive_editor'
  #gem 'ruby-debug19', :require => 'ruby-debug'
  gem 'net-ssh'
end

group :production do
#  gem 'mysql'
  gem 'therubyracer'
end
